访问者模式

定义（intent)

访问者模式，定义一组操作对结构化的对象的元素进行访问。
Vsitor模式使得可以定义一组新操作，而不用改变元素所在的类本身。


例如遍历访问一组元素，对其中的部分元素进行操作。

遍历的方式定义被访问对象的accept里，在子类中调用相应的visitor对自身进行访问。

放要对另外一些元素进行操作时，Unit及其子类不需要变化，只需要创建一个新的visitor即可。

如果出现一种新的元素，需要扩展Unit类和创建相应元素的子类，并且扩展UnitVisitor

